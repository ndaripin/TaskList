import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#553E93', 
    },
    secondary: {
      main: '#FFFFFF',
    },
  },
  shape: {
    borderRadius: 12,
  },
});

export default theme;